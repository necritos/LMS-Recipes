# GET /api/v1/public/recipes/

Listado paginado de recetas publicadas. Misma estructura que cursos, con `access_type` (`lifetime` | `timed`) y `access_days`.

Query params: `lang`, `category`, `search`, `page`, `page_size`.
